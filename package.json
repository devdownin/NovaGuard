{
  "name": "novaguard-sentinelle",
  "version": "0.1.0",
  "private": true,
  "description": "Sentinelle \u2014 the NovaGuard mobile client, built on the nocturne design system.",
  "scripts": {
    "lint": "eslint .",
    "test": "npm run test:unit && npm run test:e2e",
    "test:headed": "playwright test --headed",
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test:unit": "node --test server/",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.56.1",
    "eslint": "^10.1.0",
    "globals": "^16.4.0"
  },
  "type": "module"
}
